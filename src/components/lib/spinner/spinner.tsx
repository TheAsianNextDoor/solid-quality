// import { CircularProgress } from '@suid/material';

import type { Component } from 'solid-js';

export const Spinner: Component<any> = (props) => {
  // return <CircularProgress {...props} />;

  return <>Loading...</>;
};
